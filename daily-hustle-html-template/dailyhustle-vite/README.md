# DailyHustle — Vite + React (starter)

## Quick start

1. `npm install`
2. `npm run dev`
3. Open the URL printed by Vite (usually http://localhost:5173)

## Notes
- Bootstrap 5 and Bootstrap Icons are included via CDN in `index.html`.
- Dark mode toggle stored in localStorage. Toggle is in the header.
- This scaffold includes pages: Dashboard, Tasks, Wallet, Referrals, Leaderboard, Support, Settings.
- Replace `/api` in `src/services/api.js` with your backend endpoint.

Enjoy — ship fast! 🚀
